This project is a macOS menu bar application.
It lives in the user's menu bar and has no visible window unless settings are opened, so there should be no Dock icon either.
It runs an embedded web server to provide LLM model API services (OpenAI / Anthropic compatible).
